import React, {Component} from 'react';
import {
    Text
} from 'react-native';

export default class Payout extends Component {
    render() {
        return (
            <Text>Payout</Text>
        );
    }
}